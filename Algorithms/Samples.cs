using System;
using System.Linq;

namespace Algorithms
{
    public class Samples
    {

        /*
         * Check to see if a string has the same amount of 'a's and 'z's. 
        * The method must return a boolean and be case insensitive. 
         * The string can contain any char.
         * 
         * Examples input/output:
         * AZ("aazz") => true
         * AZ("zaazz") => false
         * AZ("aazZm") => true
         * AZ("zpzpzpp") => true // when no 'a' and 'z' is present should return true
         * AZ("zzaa") => false
         * 
         */
        public static bool AZ(string input)
        {
            return false; // Replace this line with your code
        }

    }
}
